"""
Router delle API REST (Django REST Framework). Vuoto per ora:
il primo passo (scaffold di modelli + admin) permette gia' di lavorare
da back-office; le viewset/API si aggiungeranno modulo per modulo
(clients, catalog, warehouse, invoicing, ddt, documents) nella fase
successiva, quando costruiremo il frontend.
"""
from django.urls import path

urlpatterns = []
